export function SimBanner() {
  return (
    <div className="sim-banner">
      <span className="sim-banner__icon">i</span>
      <span>
        Simulated data only. Nothing on this screen scans, captures, or attacks a real network.
      </span>
    </div>
  )
}
